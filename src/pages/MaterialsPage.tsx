import { BookOpen, Layers3 } from 'lucide-react'
import { useAppStore } from '../store/useAppStore'

export function MaterialsPage(){const s=useAppStore(x=>x.data);return <div><div className="page-heading"><div><span className="eyebrow">MATERIAIS</span><h1>Fórmulas e flashcards em um só lugar.</h1><p>Uma visão de apoio para revisão rápida.</p></div></div><div className="materials-grid"><section className="panel"><div className="panel-title"><div><span className="eyebrow">FÓRMULAS</span><h3>{s.formulas.length} registros</h3></div><BookOpen/></div><div className="formula-grid">{s.formulas.slice(0,24).map(f=><article key={f.id}><b>{f.title}</b><strong>{f.formula}</strong><small>{f.note}</small></article>)}</div></section><aside className="panel"><div className="panel-title"><div><span className="eyebrow">FLASHCARDS</span><h3>{s.flashcards.length} cartões</h3></div><Layers3/></div>{s.flashcards.slice(0,12).map(c=><div className="flash-row" key={c.id}><b>{c.front}</b><small>{c.back}</small></div>)}{!s.flashcards.length&&<p className="muted">Os flashcards da versão anterior serão exibidos aqui quando existirem.</p>}</aside></div></div>}

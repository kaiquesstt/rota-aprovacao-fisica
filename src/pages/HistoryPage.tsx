import { useAppStore } from '../store/useAppStore'
import { fmtDuration } from '../lib/date'
export function HistoryPage(){const s=useAppStore(x=>x.data);return <div><div className="page-heading"><div><span className="eyebrow">HISTÓRICO</span><h1>O que você realmente executou.</h1><p>Sessões e blocos de questões registrados no aplicativo.</p></div></div><article className="panel"><div className="table-wrap"><table><thead><tr><th>Data</th><th>Tipo</th><th>Conteúdo</th><th>Resultado</th></tr></thead><tbody>{[...s.sessions.map(x=>({date:x.date,type:'Estudo',title:x.topicTitle||'Sessão',result:fmtDuration(x.durationSeconds)})),...s.questionSessions.map(x=>({date:x.date,type:'Questões',title:x.topicTitle,result:`${x.total} questões • ${x.accuracy}%`}))].sort((a,b)=>b.date.localeCompare(a.date)).slice(0,100).map((x,i)=><tr key={i}><td>{x.date}</td><td>{x.type}</td><td>{x.title}</td><td>{x.result}</td></tr>)}</tbody></table></div></article></div>}

import { useState } from 'react'
import { Target } from 'lucide-react'
import { useAppStore } from '../store/useAppStore'

export function SimulationsPage(){const s=useAppStore(x=>x.data),add=useAppStore(x=>x.addSimulation),[total,setTotal]=useState(60),[correct,setCorrect]=useState(0);const percent=total?Math.round(correct/total*100):0;return <div><div className="page-heading"><div><span className="eyebrow">SIMULADOS</span><h1>Registre resultados sem transformar isso em uma obrigação.</h1><p>O modo de prova completa pode ser aprofundado depois; por enquanto, o foco é acompanhar desempenho.</p></div></div><div className="question-grid"><section className="panel form-panel"><span className="eyebrow">NOVO RESULTADO</span><h3>Registrar simulado</h3><div className="form-2"><label>Total<input type="number" value={total} onChange={e=>setTotal(+e.target.value)}/></label><label>Acertos<input type="number" value={correct} onChange={e=>setCorrect(+e.target.value)}/></label></div><div className="sim-preview"><strong>{percent}%</strong><span>resultado calculado</span></div><button className="cta full" onClick={()=>add(percent,total,correct)}>Salvar simulado</button></section><aside className="panel"><div className="panel-title"><div><span className="eyebrow">HISTÓRICO</span><h3>Resultados anteriores</h3></div><Target/></div>{s.simulations.slice().reverse().slice(0,12).map(x=><div className="upcoming-row" key={x.id}><div><b>{x.name||'Simulado'}</b><small>{x.date}</small></div><span>{x.percent}%</span></div>)}{!s.simulations.length&&<p className="muted">Nenhum simulado registrado.</p>}</aside></div></div>}
